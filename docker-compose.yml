version: "3.7"

services:
  revcord:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./revcord.sqlite:/app/revcord.sqlite:rw
      - ./.env:/app/.env
    env_file:
      - stack.env
    restart: unless-stopped
