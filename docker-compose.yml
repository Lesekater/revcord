version: "3.7"

services:
  revcord:
    build:
      context: .
      command: touch revcord.sqlite
      dockerfile: Dockerfile
    volumes:
      - ./revcord.sqlite:/app/revcord.sqlite:rw
      - ./.env:/app/.env
    env_file:
      - stack.env
    restart: unless-stopped
